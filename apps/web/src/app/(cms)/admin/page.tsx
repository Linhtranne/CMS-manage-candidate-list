import Link from 'next/link';
import type { Route } from 'next';
import { AdminNav } from '@/features/admin/components/admin-nav';

export default function AdminPage() {
  return <div className="space-y-6"><div><p className="text-sm font-medium text-accent">Cấu hình nội bộ</p><h1 className="mt-1 text-2xl font-bold text-text">Quản trị CMS</h1><p className="mt-2 max-w-3xl text-sm text-text-muted">Quản lý người dùng, quyền, danh mục, template, kết nối mailbox và audit. Quyền đọc nội dung ứng viên/email không được suy ra từ quyền cấu hình.</p></div><AdminNav /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3"><Link href={'/admin/users' as Route} className="rounded-lg border border-border bg-panel p-5 hover:border-accent"><h2 className="font-bold text-text">Người dùng & quyền</h2><p className="mt-2 text-sm text-text-muted">Role, scope, sensitivity và khóa phiên.</p></Link><Link href={'/admin/catalogs' as Route} className="rounded-lg border border-border bg-panel p-5 hover:border-accent"><h2 className="font-bold text-text">Danh mục</h2><p className="mt-2 text-sm text-text-muted">Ngành, nghề, tuyến visa và nguồn theo version.</p></Link><Link href={'/admin/templates' as Route} className="rounded-lg border border-border bg-panel p-5 hover:border-accent"><h2 className="font-bold text-text">Template</h2><p className="mt-2 text-sm text-text-muted">Journey template và email template có lịch sử.</p></Link><Link href={'/admin/mailbox' as Route} className="rounded-lg border border-border bg-panel p-5 hover:border-accent"><h2 className="font-bold text-text">Mailbox health</h2><p className="mt-2 text-sm text-text-muted">Theo dõi kết nối mà không lộ credential.</p></Link><Link href={'/admin/audit' as Route} className="rounded-lg border border-border bg-panel p-5 hover:border-accent"><h2 className="font-bold text-text">Audit log</h2><p className="mt-2 text-sm text-text-muted">Lọc actor, action, resource và nguồn phát sinh.</p></Link></div></div>;
}

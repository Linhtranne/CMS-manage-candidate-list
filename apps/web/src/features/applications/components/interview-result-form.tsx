'use client';

import { useId, useRef, useState } from 'react';
import type { components } from '@cms/contracts';
import { Button } from '@/components/ui/button';
import { useI18n } from '@/i18n/use-i18n';
import { translateValidationIssue } from '@/i18n/validation';
import { localizedError } from '@/i18n/errors';
import { useSaveInterviewResult } from '../services/application-queries';
import { interviewResultSchema } from '../schemas/interview-result.schema';
type Application = components['schemas']['ApplicationDetail']; type Interview = components['schemas']['Interview'];
export function InterviewResultForm({ application, interview, onSaved, onCancel, embedded = false }: { application: Application; interview: Interview; onSaved?: (interview: Interview) => void; onCancel?: () => void; embedded?: boolean }) { const { t, formatDateTime } = useI18n(); const [result, setResult] = useState<'PASS' | 'FAIL'>(interview.result === 'FAIL' ? 'FAIL' : 'PASS'); const [feedback, setFeedback] = useState(interview.feedback ?? ''); const [strengths, setStrengths] = useState(interview.strengths?.join(', ') ?? ''); const [concerns, setConcerns] = useState(interview.concerns?.join(', ') ?? ''); const [nextStep, setNextStep] = useState(interview.nextStep ?? ''); const [error, setError] = useState(''); const errorId = useId(); const feedbackRef = useRef<HTMLTextAreaElement>(null); const saveResult = useSaveInterviewResult(); const title = t('applications.interview.resultTitle', { round: interview.round }); const submit = async () => { const parsed = interviewResultSchema.safeParse({ result, feedback, strengths: strengths.split(',').map((item) => item.trim()).filter(Boolean), concerns: concerns.split(',').map((item) => item.trim()).filter(Boolean), nextStep: nextStep || null, recordedAt: new Date().toISOString(), version: interview.version }); if (!parsed.success) { setError(translateValidationIssue(t, parsed.error.issues[0], 'applications.interview.resultError')); requestAnimationFrame(() => feedbackRef.current?.focus()); return; } if (interview.scheduleStatus !== 'COMPLETED') { setError(t('applications.interview.resultRequired')); return; } try { const saved = await saveResult.mutateAsync({ applicationId: application.id, interviewId: interview.id, body: parsed.data }); onSaved?.(saved); } catch (cause) { setError(localizedError(t, cause, t('applications.interview.resultError'))); } }; return <form aria-label={title} className={embedded ? 'space-y-4' : 'space-y-4 rounded-lg border border-border bg-surface p-5'} onSubmit={(event) => { event.preventDefault(); void submit(); }}>{!embedded ? <div><h3 className="font-bold text-text">{title}</h3><p className="mt-1 text-sm text-text-muted">{t('applications.interview.interviewLabel', { date: formatDateTime(interview.scheduledAt) })}</p></div> : null}<label className="block text-sm font-semibold text-text">{t('applications.interview.result')}<select aria-label={t('applications.interview.resultAria')} name="interview-result" value={result} onChange={(event) => setResult(event.target.value as 'PASS' | 'FAIL')} className="mt-1 min-h-10 w-full rounded-control border border-border bg-panel px-3 font-normal"><option value="PASS">{t('applications.profile.pass')}</option><option value="FAIL">{t('applications.profile.fail')}</option></select></label><label className="block text-sm font-semibold text-text">{t('applications.interview.feedback')}<textarea ref={feedbackRef} aria-label={t('applications.interview.feedbackAria')} aria-invalid={Boolean(error)} aria-describedby={error ? errorId : undefined} name="interview-feedback" value={feedback} onChange={(event) => { setFeedback(event.target.value); setError(''); }} className="mt-1 min-h-24 w-full rounded-control border border-border bg-panel px-3 py-2 font-normal" /></label><label className="block text-sm font-semibold text-text">{t('applications.interview.strengths')}<input aria-label={t('applications.interview.strengthsAria')} name="interview-strengths" value={strengths} onChange={(event) => setStrengths(event.target.value)} placeholder={t('applications.interview.strengthsPlaceholder')} className="mt-1 min-h-10 w-full rounded-control border border-border bg-panel px-3 font-normal" /></label><label className="block text-sm font-semibold text-text">{t('applications.interview.concerns')}<input aria-label={t('applications.interview.concernsAria')} name="interview-concerns" value={concerns} onChange={(event) => setConcerns(event.target.value)} className="mt-1 min-h-10 w-full rounded-control border border-border bg-panel px-3 font-normal" /></label><label className="block text-sm font-semibold text-text">{t('applications.interview.nextStep')}<input aria-label={t('applications.interview.nextStepAria')} name="interview-next-step" value={nextStep} onChange={(event) => setNextStep(event.target.value)} className="mt-1 min-h-10 w-full rounded-control border border-border bg-surface px-3 font-normal" /></label>{error ? <p id={errorId} role="alert" className="text-sm font-semibold text-danger">{error}</p> : null}<div className="flex flex-wrap justify-end gap-2"><Button type="button" variant="secondary" onClick={onCancel}>{t('applications.interview.cancel')}</Button><Button type="submit" variant="primary">{t('applications.interview.resultSave')}</Button></div></form>; }

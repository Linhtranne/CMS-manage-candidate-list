'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { SavedViewBar } from '@/components/ui/saved-view-bar';
import { SavedViewMenu } from '@/components/ui/saved-view-menu';
import { useCurrentUser } from '@/lib/auth/use-current-user';
import { useListParams } from '@/hooks/use-list-params';
import { useTabKeyboard } from '@/hooks/use-tab-keyboard';
import { useI18n } from '@/i18n/use-i18n';
import { useApplications } from '../services/application-queries';
import { ApplicationDrawer } from './application-drawer';
import { AddApplicationDialog } from './add-application-dialog';
import { ApplicationTable } from './application-table';

const views = [['screening', 'applications.views.screening'], ['waiting-interview', 'applications.views.waitingInterview'], ['interviewed', 'applications.views.interviewed'], ['waiting-result', 'applications.views.waitingResult'], ['passed', 'applications.views.passed'], ['failed', 'applications.views.failed'], ['withdrawn', 'applications.views.withdrawn'], ['closed', 'applications.views.closed'], ['overdue', 'applications.views.overdue']] as const;
type ApplicationView = (typeof views)[number][0];
export function ApplicationListPage() { const { t } = useI18n(); const [addOpen, setAddOpen] = useState(false); const { params, setQuery, setView, setSelectedId } = useListParams({ defaultView: 'screening' }); const currentUser = useCurrentUser(); const view = (views.some(([id]) => id === params.view) ? params.view : 'screening') as ApplicationView; const handleTabKeyDown = useTabKeyboard(views.map(([id]) => id), setView); const query = useApplications({ query: params.query, view }); return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-accent">{t('applications.list.eyebrow')}</p><h1 className="mt-1 text-2xl font-bold text-text">{t('applications.list.title')}</h1><p className="mt-2 max-w-3xl text-sm text-text-muted">{t('applications.list.description')}</p></div><Button variant="primary" onClick={() => setAddOpen(true)}>{t('applications.list.add')}</Button></div><SavedViewBar><label className="flex min-w-0 flex-1 flex-wrap items-center gap-2 text-sm font-semibold text-text">{t('applications.list.searchLabel')}<input aria-label={t('applications.list.searchLabel')} name="application-search" value={params.query} onChange={(event) => setQuery(event.target.value)} placeholder={t('applications.list.searchPlaceholder')} className="min-h-10 min-w-0 flex-1 rounded-control border border-border bg-panel px-3 font-normal sm:w-80 sm:flex-none" /></label><label className="flex items-center gap-2 text-sm text-text-muted">{t('applications.list.stageLabel')}<select aria-label={t('applications.list.stageAria')} name="application-stage" value={view} onChange={(event) => setView(event.target.value)} className="min-h-10 rounded-control border border-border bg-panel px-3">{views.map(([id, key]) => <option key={id} value={id}>{t(key as Parameters<typeof t>[0])}</option>)}</select></label>{currentUser.data ? <SavedViewMenu resource="applications" user={currentUser.data} query={{ view, query: params.query }} onApply={(saved) => { if (typeof saved.view === 'string') setView(saved.view); if (typeof saved.query === 'string') setQuery(saved.query); }} /> : null}</SavedViewBar><div className="flex flex-wrap gap-2" role="tablist" aria-label={t('applications.list.tabLabel')} onKeyDown={handleTabKeyDown}>{views.map(([id, key]) => <button key={id} type="button" role="tab" data-tab-value={id} tabIndex={view === id ? 0 : -1} aria-selected={view === id} onClick={() => setView(id)} className={`min-h-10 rounded-control border px-3 text-sm font-semibold ${view === id ? 'border-accent bg-accent text-white' : 'border-border bg-panel text-text-muted hover:text-text'}`}>{t(key as Parameters<typeof t>[0])}</button>)}</div><ApplicationTable applications={query.data?.items ?? []} isLoading={query.isPending} error={query.error ? t('applications.list.loadError') : undefined} onRetry={() => void query.refetch()} onRowClick={(application) => setSelectedId(application.id)} /><ApplicationDrawer applicationId={params.selectedId} open={Boolean(params.selectedId)} onClose={() => setSelectedId(undefined)} /><AddApplicationDialog open={addOpen} onClose={() => setAddOpen(false)} /></div>; }

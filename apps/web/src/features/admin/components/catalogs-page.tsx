'use client';

import { useState } from 'react';
import { Button } from '@/components/ui/button';
import { Modal } from '@/components/ui/modal';
import { useAdminCatalogs, useCreateAdminCatalog, useRetireAdminCatalog } from '../services/admin-queries';
import { ErrorState } from '@/components/ui/error-state';
import { LoadingState } from '@/components/ui/loading-state';
import { StatusLabel } from '@/components/ui/status-label';
import { EmptyState } from '@/components/ui/empty-state';

export function CatalogsPage() {
  const query = useAdminCatalogs();
  const retire = useRetireAdminCatalog();
  const create = useCreateAdminCatalog();
  const [open, setOpen] = useState(false);
  const [type, setType] = useState<'INDUSTRY' | 'OCCUPATION' | 'VISA_ROUTE' | 'SOURCE'>('INDUSTRY');
  const [code, setCode] = useState('');
  const [label, setLabel] = useState('');
  const [error, setError] = useState('');
  const [retireTarget, setRetireTarget] = useState<{ id: string; label: string; version: number }>();
  const submit = () => { if (!code.trim() || !label.trim()) { setError('Nhập mã và nhãn danh mục.'); return; } setError(''); create.mutate({ type, code: code.trim().toUpperCase(), label: label.trim() }, { onSuccess: () => { setOpen(false); setCode(''); setLabel(''); }, onError: (cause) => setError(cause.message) }); };
  if (query.isPending) return <LoadingState label="Đang tải danh mục" />;
  if (query.error) return <ErrorState message="Không thể tải danh mục." onRetry={() => void query.refetch()} />;
  return <div className="space-y-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-sm font-medium text-accent">Danh mục có version</p><h1 className="mt-1 text-2xl font-bold text-text">Danh mục ngành, nghề và nguồn</h1><p className="mt-2 text-sm text-text-muted">Giá trị đã từng được sử dụng chỉ được ngừng sử dụng, không xóa khỏi lịch sử.</p></div><Button variant="primary" onClick={() => setOpen(true)}>Thêm giá trị</Button></div><section className="overflow-x-auto rounded-lg border border-border bg-panel p-5"><table className="w-full min-w-[44rem] text-left text-sm"><thead><tr className="border-b border-border text-xs uppercase tracking-wide text-text-muted"><th className="px-3 py-3">Loại</th><th className="px-3 py-3">Mã</th><th className="px-3 py-3">Nhãn</th><th className="px-3 py-3">Đang dùng</th><th className="px-3 py-3">Trạng thái</th><th className="px-3 py-3">Thao tác</th></tr></thead><tbody>{query.data?.items.length ? query.data.items.map((item) => <tr key={item.id} className="border-b border-border last:border-0"><td className="px-3 py-3 text-text-muted">{item.type}</td><td className="px-3 py-3 font-semibold text-text">{item.code}</td><td className="px-3 py-3 text-text">{item.label}</td><td className="px-3 py-3 text-text-muted">{item.usageCount}</td><td className="px-3 py-3"><StatusLabel tone={item.status === 'ACTIVE' ? 'success' : 'neutral'}>{item.status === 'ACTIVE' ? 'Đang dùng' : 'Đã ngừng'}</StatusLabel></td><td className="px-3 py-3">{item.status === 'ACTIVE' ? <button type="button" className="text-sm font-semibold text-danger underline" aria-label={item.type === 'INDUSTRY' ? 'Ngừng sử dụng ngành nghề' : `Ngừng sử dụng ${item.label}`} disabled={retire.isPending} onClick={() => setRetireTarget({ id: item.id, label: item.label, version: item.version })}>Ngừng sử dụng</button> : <span className="text-sm text-text-muted">Không thao tác</span>}</td></tr>) : <tr><td colSpan={6} className="p-4"><EmptyState title="Chưa có danh mục" description="Tạo giá trị đầu tiên để dùng trong các biểu mẫu nghiệp vụ." action={<Button variant="primary" onClick={() => setOpen(true)}>Thêm giá trị</Button>} /></td></tr>}</tbody></table></section><Modal open={open} title="Thêm giá trị danh mục" description="Giá trị mới bắt đầu ở phiên bản 1 và được ghi audit." onClose={() => setOpen(false)} size="sm" footer={<><Button onClick={() => setOpen(false)}>Hủy</Button><Button variant="primary" disabled={create.isPending} onClick={submit}>Lưu giá trị</Button></>}><div className="space-y-4">{error ? <p role="alert" className="text-sm font-semibold text-danger">{error}</p> : null}<label className="block text-sm font-semibold text-text">Loại<select aria-label="Loại danh mục" name="loai-danh-muc" value={type} onChange={(event) => setType(event.target.value as typeof type)} className="mt-1 min-h-10 w-full rounded-control border border-border bg-panel px-3 font-normal"><option value="INDUSTRY">Ngành</option><option value="OCCUPATION">Nghề</option><option value="VISA_ROUTE">Tuyến tư cách</option><option value="SOURCE">Nguồn ứng viên</option></select></label><label className="block text-sm font-semibold text-text">Mã<input aria-label="Mã danh mục" name="ma-danh-muc" value={code} onChange={(event) => setCode(event.target.value)} className="mt-1 min-h-10 w-full rounded-control border border-border bg-panel px-3 font-normal" /></label><label className="block text-sm font-semibold text-text">Nhãn<input aria-label="Nhãn danh mục" name="nhan-danh-muc" value={label} onChange={(event) => setLabel(event.target.value)} className="mt-1 min-h-10 w-full rounded-control border border-border bg-panel px-3 font-normal" /></label></div></Modal><Modal open={Boolean(retireTarget)} title="Ngừng sử dụng danh mục?" description="Danh mục không bị xoá khỏi dữ liệu lịch sử." onClose={() => setRetireTarget(undefined)} size="sm" footer={<><Button onClick={() => setRetireTarget(undefined)}>Hủy</Button><Button variant="primary" disabled={retire.isPending} onClick={() => { if (!retireTarget) return; retire.mutate({ id: retireTarget.id, body: { version: retireTarget.version } }, { onSuccess: () => setRetireTarget(undefined) }); }}>Xác nhận ngừng</Button></>}><p className="text-sm text-text">{retireTarget?.label}</p></Modal></div>;
}

'use client';

import { useState } from 'react';
import { ErrorState } from '@/components/ui/error-state';
import { LoadingState } from '@/components/ui/loading-state';
import { EmptyState } from '@/components/ui/empty-state';
import { useAdminAudit } from '../services/admin-queries';

export function AuditLogPage() {
  const [resourceId, setResourceId] = useState('');
  const [action, setAction] = useState('');
  const [actorId, setActorId] = useState('');
  const [from, setFrom] = useState('');
  const [to, setTo] = useState('');
  const query = useAdminAudit({ resourceId: resourceId || undefined, action: action || undefined, actorId: actorId || undefined, from: from || undefined, to: to || undefined });
  if (query.isPending) return <LoadingState label="Đang tải audit log" />;
  if (query.error) return <ErrorState message="Không thể tải audit log." onRetry={() => void query.refetch()} />;
  const items = query.data?.items ?? [];
  return <div className="space-y-6"><div><p className="text-sm font-medium text-accent">Truy vết bất biến</p><h1 className="mt-1 text-2xl font-bold text-text">Audit log</h1><p className="mt-2 max-w-3xl text-sm text-text-muted">Server lọc theo quyền; nội dung nhạy cảm được mask và không có thao tác sửa/xóa.</p></div><section className="grid gap-4 rounded-lg border border-border bg-panel p-4 sm:grid-cols-2 xl:grid-cols-5"><label className="block min-w-0 text-sm font-semibold text-text">Từ ngày<input aria-label="Từ ngày audit" name="tu-ngay-audit" type="date" value={from} onChange={(event) => setFrom(event.target.value)} className="mt-1 block min-h-10 w-full min-w-0 rounded-control border border-border bg-panel px-3 font-normal" /></label><label className="block min-w-0 text-sm font-semibold text-text">Đến ngày<input aria-label="Đến ngày audit" name="den-ngay-audit" type="date" value={to} onChange={(event) => setTo(event.target.value)} className="mt-1 block min-h-10 w-full min-w-0 rounded-control border border-border bg-panel px-3 font-normal" /></label><label className="block min-w-0 text-sm font-semibold text-text">Actor<select aria-label="Actor audit" name="actor-audit" value={actorId} onChange={(event) => setActorId(event.target.value)} className="mt-1 block min-h-10 w-full min-w-0 rounded-control border border-border bg-panel px-3 font-normal"><option value="">Tất cả actor</option><option value="u-manager">Lê Thu Hà</option><option value="u-config">Phạm Đức Long</option><option value="u-recruiter">Nguyễn Minh Anh</option></select></label><label className="block min-w-0 text-sm font-semibold text-text">Resource ID<input aria-label="Resource ID" name="resource-id" value={resourceId} onChange={(event) => setResourceId(event.target.value)} className="mt-1 block min-h-10 w-full min-w-0 rounded-control border border-border bg-panel px-3 font-normal" placeholder="candidate-01" /></label><label className="block min-w-0 text-sm font-semibold text-text">Action<input aria-label="Audit action" name="audit-action" value={action} onChange={(event) => setAction(event.target.value)} className="mt-1 block min-h-10 w-full min-w-0 rounded-control border border-border bg-panel px-3 font-normal" placeholder="EMAIL_SENT" /></label></section><section className="overflow-x-auto rounded-lg border border-border bg-panel p-5">{items.length ? <table className="w-full min-w-[58rem] text-left text-sm"><thead><tr className="border-b border-border text-xs uppercase tracking-wide text-text-muted"><th className="px-3 py-3">Thời gian</th><th className="px-3 py-3">Actor</th><th className="px-3 py-3">Action</th><th className="px-3 py-3">Resource</th><th className="px-3 py-3">Nguồn</th><th className="px-3 py-3">Tóm tắt</th></tr></thead><tbody>{items.map((item) => <tr key={item.id} className="border-b border-border last:border-0"><td className="px-3 py-3 text-text-muted">{new Date(item.occurredAt).toLocaleString('vi-VN')}</td><td className="px-3 py-3 text-text">{item.actor.name}</td><td className="px-3 py-3 font-semibold text-text">{item.action}</td><td className="px-3 py-3 text-text-muted">{item.resourceType} · {item.resourceId}</td><td className="px-3 py-3 text-text-muted">{item.source}</td><td className="px-3 py-3 text-text">{item.summary}</td></tr>)}</tbody></table> : <EmptyState title="Không có bản ghi audit" description="Thử nới bộ lọc hoặc quay lại sau khi có hoạt động được ghi nhận." />}</section></div>;
}
